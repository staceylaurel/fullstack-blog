import { Router } from "./index";

const one = (id: number) => Router(res.json("TEST LOGIN"));

const insert = (authorid: number, title: string, content: string) =>
  Router(res.json("TEST LOGIN"));


export default {
  one,
  insert
};