import { Router } from "./index";

const one = (id: number) => Router(res.json("TEST REGISTER"));

const insert = (authorid: number, title: string, content: string) =>
  Router(res.json("TEST REGISTER"));


export default {
  one,
  insert
};